#include <iostream>
using namespace std;

int main () {
    float a,b,c;

    cout << "Enter coefficients a, b and c: ";

    cin >> a >> b >> c;

    float discriminant = b * b - 4 * a * c;

    if (discriminant > 0) {

    } else if (discriminant == 0) {

    } else {

    }

}